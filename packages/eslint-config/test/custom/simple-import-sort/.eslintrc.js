module.exports = {
	rules: {
		'simple-import-sort/imports': [
			'warn',
			{
				groups: [
					// react等包放在首位.
					['^react', '^@?\\w'],
					// 绝对路径导入.
					['^(@|components)(/.*|$)'],
					// 相对路径导入
					[
						// 文件夹下的文件
						'^\\.\\.(?!/?$)',
						'^\\.\\./?$',
						// 当前同级文件下的文件
						'^\\./(?=.*/)(?!/?$)',
						'^\\.(?!/?$)',
						'^\\./?$',
					],
					// 其他的导入.
					['^\\u0000'],
					// 样式.
					['^.+\\.?(css)$', '^.+\\.?(scss)$', '^.+\\.?(less)$'],
				],
			},
		],
	},
};
